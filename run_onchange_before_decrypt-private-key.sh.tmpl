#!/bin/sh

if [ ! -f "/home/maximo/.config/chezmoi/key.txt" ]; then
    mkdir -p "/home/maximo/.config/chezmoi"
    chezmoi age decrypt --output "/home/maximo/.config/chezmoi/key.txt" --passphrase "{{ .chezmoi.sourceDir }}/key.txt.age"
    chmod 600 "/home/maximo/.config/chezmoi/key.txt"
fi
